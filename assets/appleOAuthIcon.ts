import type { ImageSourcePropType } from 'react-native'

/** OAuth ボタン用 Apple マーク（黒シルエット PNG を tint で白表示） */
export const appleOAuthIconSource = require('./images/apple_logo.png') as ImageSourcePropType
